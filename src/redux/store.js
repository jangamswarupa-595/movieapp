// src/redux/store.js
/*import { configureStore } from "@reduxjs/toolkit";
import rootReducer from "./Reducers";

const store = configureStore({
  reducer: rootReducer,
});

export default store;*/

/*import { configureStore } from "@reduxjs/toolkit";
import rootReducer from "./rootReducer";

const store = configureStore({
  reducer: rootReducer,
});

export default store;*/

// src/Redux/rootReducer.js
// src/Redux/store.js
// src/Redux/store.js
// src/Redux/store.js
/*import { configureStore } from '@reduxjs/toolkit';
import movieReducer from './movieReducer'; // Ensure this path is correct

const store = configureStore({
  reducer: {
    movies: movieReducer,
  },
});

export default store;*/

//2

/*import { configureStore } from '@reduxjs/toolkit';
import movieReducer from './movieReducer'; // Ensure this path is correct

const store = configureStore({
  reducer: {
    movies: movieReducer,
  },
});

export default store;*/
//3

/*import { configureStore } from '@reduxjs/toolkit';
import movieReducer from './MovieReducer';// Ensure the file name and path are correct

const store = configureStore({
  reducer: {
    movies: movieReducer,
  },
});

export default store;*/

// src/redux/store.js

// src/redux/store.js
import { configureStore } from '@reduxjs/toolkit';
import movieReducer from './redux/MovieReducer.js'; // Ensure this path is correct

const store = configureStore({
  reducer: {
    movies: movieReducer,
  },
});

export default store;

